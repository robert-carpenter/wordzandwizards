{
  "name": "spellcast-clone",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:activity-test": "node scripts/dev-activity-test.mjs",
    "build": "npm run build:client && npm run build:server",
    "build:client": "vite build",
    "build:server": "vite build --config vite.server.config.ts",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "start": "node dist/server/index.mjs",
    "test": "vitest",
    "test:run": "vitest --run",
    "verify:client-secrets": "node scripts/check-client-secrets.mjs",
    "verify": "npm run typecheck && npm run test:run && npm run build && npm run verify:client-secrets"
  },
  "dependencies": {
    "@discord/embedded-app-sdk": "^2.5.0",
    "@fontsource/play": "^5.2.8",
    "@fortawesome/fontawesome-free": "^7.0.1",
    "@types/three": "^0.181.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "gsap": "^3.13.0",
    "socket.io": "^4.7.5",
    "socket.io-client": "^4.7.5",
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.30",
    "@types/supertest": "^6.0.3",
    "supertest": "^7.1.4",
    "typescript": "^5.3.3",
    "vite": "^5.0.8",
    "vitest": "4.0.14"
  }
}
